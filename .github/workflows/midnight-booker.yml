name: Midnight Booker

on:
  schedule:
    # 11:00pm EDT (3:00 UTC) — generous 60min buffer absorbs GitHub Actions
    # scheduled-workflow delays (observed up to ~55min); script then sleeps until midnight.
    # In winter (EST) this fires at 10:00pm ET; the script will sleep ~2hr until midnight.
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  book:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Attempt midnight booking
        run: python scheduler.py
        env:
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
          LOCATION_ID: ${{ secrets.LOCATION_ID }}
          PARTY_SIZE: ${{ secrets.PARTY_SIZE }}
          FIVE_IRON_EMAIL: ${{ secrets.FIVE_IRON_EMAIL }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          GOOGLE_CALENDAR_ID: ${{ secrets.GOOGLE_CALENDAR_ID }}
          GOOGLE_EVENT_COLOR: ${{ secrets.GOOGLE_EVENT_COLOR }}
      - name: Commit updated targets.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add targets.json
          git diff --staged --quiet || git commit -m "chore: update targets after booking attempt" && git push
